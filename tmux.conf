set-option -s exit-unattached off
set-option -g base-index 1
set-option -g remain-on-exit on
set-option -g history-limit 10000

new-session -d -s systemux -n main hello
new-window -d -n shell "bash --login"
